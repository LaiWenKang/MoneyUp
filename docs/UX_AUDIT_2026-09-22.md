# UX audit and elevation plan — 22 September 2026

First principles for a private money app: the person wants to know **what is left**, **what just happened**, and **what to do next**, in under two seconds, without reading a paragraph. Every screen should lead with one figure or one action, carry status in a glyph plus a short label (never colour alone), and keep explanation one glyph away. Financial precision is never traded for polish: exact amounts, currencies and dates stay in text with monospaced digits.

## Shipped in this cycle (0.7.2, builds 1060.1 → 1062.x)

| Area | Change | Why |
|---|---|---|
| Log | The currency beside the amount is a control: switch the paying account by currency, or *Paid in another currency…* to convert at the charged rate (saved rate offered for the entry's day). The account moves by exactly what it was charged; the original amount and rate are written into the note. | The grey code read as a caption; people could not see how to log a spend in another currency. Keeping the ledger single-currency per posting preserves per-currency balancing. |
| Log | Recent entries as one-tap capsules; one-row Smart entry with fill arrow or receipt glyph; helper paragraphs behind the explainer. | Fewer words between the amount and Save. |
| Log / History | Widget captures made while locked open straight in Log after unlock; History's capture card has *Open in Log* and *Discard*. | The inbox review step was both confusing and, in practice, stuck. |
| History | Filter and Clear are glyph chips on the scope row (count badge when filters are active; Clear only when there is something to clear). The separate filter card is gone. | Three stacked control cards above the first transaction was chrome, not content. |
| Everywhere | Section explanations sit on the header line as one glyph (`MoneyUpSectionHeader`) instead of floating under the card. 14 sections converted. | The orphan ⓘ under each card read as a stray control. |
| Everywhere | One shared empty / loading / warning placeholder; Today's insights row in the card system; labeled same-scale flow bars; Settings row glyphs; per-category Reports chart; titled backup warning. | Cohesion: the same state looks the same on every screen. |
| Insights | Mark reviewed, Reviewed group, Restore, Mark all; keyed on the recurring series. | Findings could not be cleared. |

## Shipped in the fourth pass (same day)

| Area | Change |
|---|---|
| Today | A hero card leads with *Left this month* across the pinned categories and *Today* underneath, shown only when every pinned category reports in one currency so the figure is never a mixed sum. The board follows as the breakdown. |
| History | Each row's kind glyph carries a small, stable category tint dot, so a day scans by colour before any label is read. |
| Assets | Accounts are grouped by currency with a subtotal on each group header. *Save today's net-worth snapshot* is a camera glyph in the toolbar; the confirmation briefly replaces the glyph. |
| Log | The Saved banner names the posted amount (masked when exact amounts are hidden) beside Undo, with the shared confirmation spring; Reduce Motion falls back to a fade. |
| Insights | The privacy card is full on the first visit and a single header-line glyph afterwards, so findings start at the top. |
| First run | Plan and Goals empty states carry the brand illustrations. |
| Onboarding | Every benefit line is one sentence. |
| Backup and recovery | A *Recovery notes* section lists redacted codes for anything MoneyUp could not finish, such as a capture that could not be handed to Log. |
| Tooling | StoreKit purchase test waits on deadlines instead of fixed counts. The public release workflow gains a confirmed *withdraw* operation that cancels the pending review submission so a new build can be prepared. The tester workflow waits up to ten minutes for Apple to publish an uploaded build before failing. CI runs once per change (push triggers only on main). |

## Deliberately not changed, with reasons

- **Calendar day dots and tap-to-History.** The month grid is the system graphical date picker; its dots cannot be tinted per kind without replacing the control. The selected day's flow already opens in the same screen. A custom grid is a separate design task.
- **Goal projected completion date.** A projection needs a contribution cadence the goal does not store; the goal detail's simulator owns that input. Adding a guessed date to the row would be false precision.
- **Plan pace chips as icon-only.** The system segmented control does not render mixed icon/label segments reliably across Dynamic Type; the current text segments stay.
- **Narrowing the validator's Log view pin.** The Quick Log input boundary pin is a reviewed security control with its own test suite; loosening it is a reviewer decision, not a UI change.
- **Device VoiceOver and large-text pass.** Render tests cover accessibility sizes for Insights, Log and History, but a physical-device walkthrough remains open.

## Next, in priority order

1. **Today hero.** Lead with one figure: *left this month* across pinned categories, with the day's share underneath, and move the date row's ⓘ onto the header line. The pinned board becomes the detail beneath the figure.
2. **History rows.** A small category tint dot beside the kind glyph and a right-aligned amount with the currency code muted, so scanning a day is a single vertical line of numbers.
3. **Plan → Budget.** Replace the *Spending · month marker* legend row with an inline caption under the bar and make the pace segment control icon-plus-label only when selected (same rule as the scope chips).
4. **Assets.** Group accounts by currency with a per-currency subtotal row; move *Save today's snapshot* to the toolbar as a camera glyph with a label on tap confirmation.
5. **Success feedback.** After Save in Log, animate the saved amount into the *Saved* banner (spring, Reduce Motion aware) so the eye follows the money; today the banner appears without motion.
6. **Onboarding.** Cut each step's card copy to one line per benefit; the illustrations already carry the mood.
7. **Motion.** Adopt the shared `MoneyUpMotion.disclosure` spring on all expandable cards; today a few use the default easing.
8. **Large text.** Verify the new History scope row and Log currency menu at accessibility sizes on device (the render tests cover XL text for Insights and Log).

## Engineering items found on the way

- `DeveloperSupportTests` (StoreKit purchase) is timing-dependent on the simulator's StoreKit daemon and fails intermittently in CI; it should poll the transaction stream with a bounded deadline instead of fixed sleeps.
- `appstore_release.py prepare` cannot replace the build of a version in `WAITING_FOR_REVIEW`; add an explicit, confirmed *withdraw and resubmit* operation so the owner can do this from the workflow.
- Apple needs about eight minutes after upload before a build is API-visible; the tester workflow could poll `inspect` before `inherit-compliance` instead of failing.
- The Paid Apps Agreement is recorded as not accepted; live tips cannot complete until the Account Holder accepts it in App Store Connect.
