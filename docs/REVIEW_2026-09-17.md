# Financial correctness and recording review — 2026-09-17

Baseline: `3791b0128115ff815ca06b5509827728d17e54e7`.

## Correctness repairs

- Validate all allowance periods after adding usage, so backdating cannot spend rollover capacity already consumed by later usage.
- Parse fixed CSV date formats with Gregorian/POSIX semantics while retaining regional day/month ordering.
- Use receipt currency evidence for three-decimal money. Keep explicit grouping, require review for ambiguous single separators, and avoid classifying labelled fractional totals as long identifiers. Currency evidence is computed once per document.
- Freeze the original quoted exchange rate with new net-worth evidence. Inverse conversion and validation share exact division and currency rounding. Existing applied-rate-only snapshots remain readable.
- Validate frozen budget attribution against its stored offset/day, independent of later tzdata changes. Reject malformed dates, zones, offsets and postings.
- Add the missing budget-intelligence translations and scan metadata localization references in Sources.

## Recording, accessibility and presentation

- Retain preloads during debounced lookups, disable stale actions, restore single-CJK-character searches, and preserve expanded details when applying a preload.
- Apply the financial-value motion policy to amount-driven charts/dials and the shared Reduce Motion policy in the transaction editor.
- Use adaptive income/warning/danger roles, require 4.5:1 contrast for status text, and align ordinary expense rows with the expense chart color.
- Unify navigation surfaces and primary-list backgrounds while preserving native controls and gestures.
- Improve pace-bar VoiceOver values, receipt removal hit targets and exchange-rate default/validation guidance.
- Declare the imported XLSX type in the generated app bundle.

## Local evidence before PR CI

- 530 package tests passed (477 XCTest plus 53 Swift Testing).
- 729 app tests passed; subsequent overlapping targeted runs passed 64, 11 and 5 tests, including the added built-bundle declaration regression and final receipt/navigation refinements.
- 109 script tests, 17 signpost-validator tests and 60 platform-validator tests passed.
- Seven source/release validators passed; Debug and Release Simulator builds and built-bundle checks passed.
- Eleven optimized performance cases passed; the final receipt path was retested separately. Timings are observational under shared-machine load, not a physical-device performance acceptance result.
- Thirty representative bilingual/light/dark/large-text screenshots were visually reviewed. The screenshot-capture test retained from the local workspace is included in this candidate and its declaration accounting.

Targeted runs overlap the full suites and must not be added as unique-test counts. Exact PR/main CI and upload receipts are separate release evidence.

## Boundaries retained

SQLCipher key derivation and cloud-backup authentication policy are unchanged. A raw-key migration needs dedicated compatibility, interruption and recovery validation. New snapshots are backwards-readable by this candidate; downgrade compatibility with newly stored inverse-rate evidence is not asserted.

An iOS 26.5 Simulator warning during repeated appearance changes was traced to UIKit's internal animation thread. Checked palette values remain valid and no corrupted colors were observed in reviewed screenshots. A local SwiftUI animation override did not eliminate the warning and was reverted. Physical-device appearance, VoiceOver, sustained scrolling, power and thermal acceptance remain open.

No public App Store release is authorized by this review. The subsequent owner instruction authorizes push, merge and delivery to App Store Connect.
